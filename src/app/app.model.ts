export interface TodoItemDTO {
    value: string;
    isDone: boolean;
    timestamp: Date;
}

export interface TodoItem {
    id: number;
    value: string;
    isDone: boolean;
    timestamp: Date;
}
